directories=(
    ~/.gem/ruby/2.7.0/bin
    ~/.gem/ruby/3.0.0/bin
    ~/.local/bin
    /opt/riscv/bin
    ~/.cargo/bin
)

for directory in $directories; do
    [ -d $addition ] && PATH=$PATH:$directory
done

